{
  "name": "finsandpins-intake",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute intake --local --file=./schema.sql",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.33.1"
  }
}

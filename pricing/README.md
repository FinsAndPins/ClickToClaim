# Pricing harness mirrors

Static ClickToPrice / ClickToMatch pages mirrored here when we need a safe UI upgrade
without write access to `FinsAndPins/PreparingInventory`, or for temporary Pages hosting.

Each folder keeps the original Firebase `TEST_RUN_ID` so Lexi/Steve reviews are not reset.
